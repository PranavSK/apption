import { type Meta, type StoryObj } from '@storybook/react';

import { UserOAuthButtons } from '.';

export default {
  component: UserOAuthButtons,
} as Meta<typeof UserOAuthButtons>;
export const Default: StoryObj<typeof UserOAuthButtons> = {};
